# 模块 3：哈希表与缓存

哈希表通过哈希函数把键映射到桶，平均查询、插入、删除为 `O(1)`，最坏可退化。正确性还依赖 `equals` 与 `hashCode` 的一致性：相等对象必须有相同哈希值。

LRU 缓存需要同时做到 `O(1)` 查询与 `O(1)` 更新：哈希表定位节点，双向链表维护新旧顺序。访问节点后将其移到表头，容量超限时淘汰表尾节点。这是“组合数据结构”的经典范例。

练习：`LruCacheExercise`。

